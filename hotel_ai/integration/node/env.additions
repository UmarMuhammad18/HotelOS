# Added by ai/python-advisor-integration
# Point at the Python advisor service. Leave unset in dev to force the
# fallback (local keyword orchestrator) for quick smoke tests.
PYTHON_ADVISOR_URL=http://localhost:9000/v1
PYTHON_ADVISOR_TOKEN=change-me-internal-token
PYTHON_ADVISOR_TIMEOUT_MS=5000
